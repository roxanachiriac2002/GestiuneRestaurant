@page
@model GestiuneRestaurant.Pages.Comenzi.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Comanda</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Comanda.ComandaId" />
            <div class="form-group">
                <label asp-for="Comanda.MasaId" class="control-label"></label>
                <select asp-for="Comanda.MasaId" class="form-control" asp-items="ViewBag.MasaId"></select>
                <span asp-validation-for="Comanda.MasaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
